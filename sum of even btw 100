#include <stdio.h>

int main() 
{
    int sum = 0, i;

    for (i = 2; i <= 100; i += 2)
{
        sum += i;  // Add the even number to the sum
    }

    printf("Sum of all even numbers between 1 and 100 = %d\n", sum);

    return 0;
}
