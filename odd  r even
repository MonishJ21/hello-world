#include<stdio.h>
int main()
{
    int x;
    printf("entre the number x:");
    scanf("%d",&x);
    
    if(x%2==0)
    printf("The number is even");
    else
    printf("The numberr is odd");
    
    return 0;
}
