#include<stdio.h>
int main()
{
    int x,y;
    printf("entre the number x:");
    scanf("%d",&x);
    printf("entre the number y:");
    scanf("%d",&y);
    
    if(x>y)
    printf("x is greater then y");
    else
    printf("y is greater than x");
    
    return 0;
}
